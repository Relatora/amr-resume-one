# Password for the in-page editor's "Owner access" gate.
# NEXT_PUBLIC_ means this value is inlined into the browser bundle - it is a
# friction gate, not a secret. Copy this file to .env and set your own value,
# and add the same variable in the Vercel project settings for the deployed site.
NEXT_PUBLIC_EDIT_PASSWORD=
